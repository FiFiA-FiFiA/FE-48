const nav__top = document.querySelector('[nav__top]');
const nav__left = document.querySelector('[nav__left]');

function opan() {
  nav__top.classList.toggle('active');
  nav__left.classList.toggle('active');
}